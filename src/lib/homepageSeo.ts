/** Shared homepage SEO copy — mirrored in index.html static fallback for crawlers. */

export const HOME_PAGE_TITLE = 'HireSchema — AI Remote Job Matching';

export const HOME_META_DESCRIPTION =
  'AI remote job matching that scores live listings against your resume. Free: 10 daily remote job matches. Pro ($19/mo): resume tailoring, cold emails, cover letters, and interview prep.';

export const HOME_OG_DESCRIPTION =
  'AI remote job matching with 10 free daily scored remote job matches. Pro from $19/mo.';
